import React from 'react';
import style from './Messages/Messages.module.css';
import generelContentStyle from './../Profile/Profile.module.css';

export const Music = (props: any) => {
    return (
        <div className={generelContentStyle.section}>
            <div className={generelContentStyle.wrapper}>Music</div>
        </div>
    )
}